# Paramètres de base
listen_addresses = '*'
port = 5432
max_connections = 100

# Paramètres WAL pour la réplication
wal_level = replica
max_wal_senders = 10
wal_keep_size = 1GB
max_replication_slots = 10
hot_standby = on

# Authentification
password_encryption = scram-sha-256